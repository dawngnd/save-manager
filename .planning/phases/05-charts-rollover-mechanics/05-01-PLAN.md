---
phase: 05-charts-rollover-mechanics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/DepositList.tsx
  - frontend/src/components/App.tsx
  - frontend/src/components/RolloverForm.tsx
autonomous: true
requirements:
  - DEP-02
must_haves:
  truths:
    - "Nút 'Tái tục' chỉ được hiển thị đối với các thẻ khoản gửi đã đến hạn hoặc quá hạn (diffDays <= 0) trong panel chi tiết (D-01)."
    - "Form Tái tục được điền sẵn với số tiền gốc bằng khoản gốc cũ, lãi suất bằng lãi suất cũ, ngày tạo bằng ngày đáo hạn cũ, và ngày đáo hạn mới được cộng thêm 1 năm (D-02)."
    - "Gửi yêu cầu API 'rollover_deposit' thành công lên backend và danh sách khoản gửi được làm mới ngay sau đó."
  artifacts:
    - "frontend/src/components/RolloverForm.tsx"
  key_links:
    - "Tệp frontend/src/components/App.tsx liên kết component RolloverForm để xử lý logic hiển thị Bottom Sheet tái tục."
    - "Tệp frontend/src/components/DepositList.tsx truyền sự kiện yêu cầu tái tục lên App.tsx."
---

<objective>
Xây dựng và tích hợp tính năng Tái tục (Rollover) trên giao diện frontend. Người dùng có thể chọn tái tục đối với khoản gửi đã đến hạn, form sẽ tự động tính toán thông tin mặc định theo quy định và gửi yêu cầu cập nhật lên backend API.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-charts-rollover-mechanics/05-CONTEXT.md
@.planning/phases/05-charts-rollover-mechanics/05-RESEARCH.md
@.planning/phases/05-charts-rollover-mechanics/05-PATTERNS.md
</context>

<tasks>
<task type="auto">
  <name>Tạo component RolloverForm.tsx xử lý logic và UI nhập liệu tái tục</name>
  <files>
    - frontend/src/components/RolloverForm.tsx
  </files>
  <read_first>
    - .planning/phases/05-charts-rollover-mechanics/05-RESEARCH.md
    - .planning/phases/05-charts-rollover-mechanics/05-PATTERNS.md
    - frontend/src/components/DepositForm.tsx
  </read_first>
  <behavior>
    - Xây dựng component RolloverForm dạng Bottom Sheet sử dụng cấu trúc giao diện đồng bộ với DepositForm.
    - Điền mặc định các thông số từ khoản gửi cũ: Số tiền gốc mới = Số tiền cũ, Lãi suất mới = Lãi suất cũ, Ngày tạo mới = Ngày đáo hạn cũ (D-02).
    - Tính toán tự động ngày đáo hạn mới bằng cách cộng 1 năm vào ngày đáo hạn cũ, kèm hàm bảo vệ năm nhuận (29/02 -> 28/02 nếu năm tiếp theo không nhuận) (D-02).
    - Thực hiện kiểm tra lỗi (validation) đối với số tiền, lãi suất, ngày bắt đầu và ngày đáo hạn trước khi submit.
    - Gọi API POST hành động `rollover_deposit` lên GAS backend để cập nhật và đóng form sau khi thành công.
  </behavior>
  <action>
    Tạo mới tệp frontend/src/components/RolloverForm.tsx với cấu hình UI và logic đầy đủ như mô tả trong 05-PATTERNS.md (per D-02).
  </action>
  <verify>
    <automated>test -f frontend/src/components/RolloverForm.tsx && grep -q "rollover_deposit" frontend/src/components/RolloverForm.tsx</automated>
  </verify>
  <done>Tệp RolloverForm.tsx được tạo thành công với cấu trúc logic chính xác.</done>
</task>

<task type="auto">
  <name>Tích hợp nút Tái tục vào Panel chi tiết và quản lý trạng thái form tại App.tsx</name>
  <files>
    - frontend/src/components/DepositList.tsx
    - frontend/src/components/App.tsx
  </files>
  <read_first>
    - .planning/phases/05-charts-rollover-mechanics/05-PATTERNS.md
    - frontend/src/components/DepositList.tsx
    - frontend/src/components/App.tsx
  </read_first>
  <behavior>
    - Chỉnh sửa DepositList.tsx để thêm nút "Tái tục" bên cạnh nút "Đóng" trong Panel chi tiết (D-01). Nút này chỉ xuất hiện khi diffDays <= 0 (khoản gửi đã đến hạn hoặc quá hạn).
    - Khi bấm "Tái tục", kích hoạt callback truyền ngược lên App.tsx và đóng Bottom Sheet chi tiết hiện tại.
    - Chỉnh sửa App.tsx để định nghĩa state chứa thông tin khoản gửi cần tái tục, render `RolloverForm` và truyền callback reload danh sách sau khi tái tục thành công.
  </behavior>
  <action>
    Sửa đổi frontend/src/components/DepositList.tsx và frontend/src/components/App.tsx để kết nối luồng tương tác tái tục của người dùng (per D-01).
  </action>
  <verify>
    <automated>grep -q "onTriggerRollover" frontend/src/components/DepositList.tsx && grep -q "RolloverForm" frontend/src/components/App.tsx</automated>
  </verify>
  <done>Kết nối tương tác tái tục giữa danh sách khoản gửi, form tái tục và luồng chính của App thành công.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client Browser -> GAS Web App API | Thông tin rollover gửi qua HTTP POST tới Google Apps Script API endpoint. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| SEC-03 | Denial of Service | Submit Button | Low | Mitigated | Client-side disabling của nút submit trong quá trình gọi API để ngăn chặn người dùng bấm nhiều lần sinh ra nhiều giao dịch trùng lặp. |
| SEC-04 | Spoofing / Tampering | Rollover API Request | Medium | Mitigated | API backend (Code.js) thực hiện LockService để đồng bộ luồng ghi và kiểm tra ID hợp lệ trước khi thực hiện cập nhật. |
</threat_model>
