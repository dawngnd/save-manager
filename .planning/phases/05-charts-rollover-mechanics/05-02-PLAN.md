---
phase: 05-charts-rollover-mechanics
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - frontend/package.json
  - frontend/src/components/GrowthChart.tsx
  - frontend/src/components/App.tsx
  - backend/Code.js
autonomous: true
requirements:
  - STAT-01
must_haves:
  truths:
    - "Cài đặt thành công thư viện chart.js vào frontend/package.json."
    - "Biểu đồ tăng trưởng tổng tài sản hiển thị đúng dạng step-wise (D-03), kéo dài từ ngày hiện tại tới ngày đáo hạn xa nhất của các khoản gửi (D-04)."
    - "Biểu đồ chỉ hiển thị khi Mini App được mở với tham số view=chart hoặc start_param=chart (D-05)."
    - "Tin nhắn phản hồi từ Telegram Bot khi nhận lệnh /chart chứa nút bấm Web App URL được gắn tham số view=chart chính xác (D-06)."
  artifacts:
    - "frontend/src/components/GrowthChart.tsx"
  key_links:
    - "Tệp frontend/src/components/App.tsx liên kết tới GrowthChart để vẽ đồ thị tăng trưởng."
    - "Tệp backend/Code.js tích hợp lệnh /chart để dẫn link trực tiếp tới biểu đồ của Web App."
---

<objective>
Tích hợp biểu đồ tăng trưởng tài sản lũy tiến (step-wise) bằng thư viện Chart.js hiển thị trên giao diện của người dùng. Cập nhật Telegram Bot hỗ trợ lệnh /chart để dẫn hướng trực tiếp người dùng tới màn hình biểu đồ.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-charts-rollover-mechanics/05-CONTEXT.md
@.planning/phases/05-charts-rollover-mechanics/05-RESEARCH.md
@.planning/phases/05-charts-rollover-mechanics/05-PATTERNS.md
</context>

<tasks>
<task type="auto">
  <name>Cài đặt thư viện Chart.js và tạo GrowthChart.tsx với thuật toán dự phóng step-wise</name>
  <files>
    - frontend/package.json
    - frontend/src/components/GrowthChart.tsx
  </files>
  <read_first>
    - .planning/phases/05-charts-rollover-mechanics/05-RESEARCH.md
    - .planning/phases/05-charts-rollover-mechanics/05-PATTERNS.md
    - frontend/package.json
  </read_first>
  <behavior>
    - Chạy lệnh cài đặt `chart.js` trong thư mục frontend/.
    - Tạo mới tệp GrowthChart.tsx. Đăng ký các module Chart.js cần thiết để tối ưu dung lượng đóng gói đơn file.
    - Viết thuật toán `generateStepWiseGrowthData` sinh dữ liệu tổng giá trị tài sản từng ngày (gốc và lãi lũy tiến) từ ngày hôm nay tới ngày đáo hạn lớn nhất của tất cả các khoản gửi đang hoạt động (D-03, D-04).
    - Khởi tạo Chart.js trên canvas qua useRef, cấu hình style Dark Mode và thực hiện gọi `.destroy()` trong hàm cleanup để dọn dẹp bộ nhớ (D-03).
  </behavior>
  <action>
    Cài đặt package chart.js và tạo mới component GrowthChart.tsx có đầy đủ logic hiển thị đồ thị và tính toán step-wise (per D-03, D-04).
  </action>
  <verify>
    <automated>cd frontend && grep -q "chart.js" package.json && test -f src/components/GrowthChart.tsx</automated>
  </verify>
  <done>Cài đặt thành công chart.js và hoàn thiện component GrowthChart.tsx.</done>
</task>

<task type="auto">
  <name>Tích hợp định tuyến tham số biểu đồ tại App.tsx và Webhook tại backend/Code.js</name>
  <files>
    - frontend/src/components/App.tsx
    - backend/Code.js
  </files>
  <read_first>
    - .planning/phases/05-charts-rollover-mechanics/05-RESEARCH.md
    - .planning/phases/05-charts-rollover-mechanics/05-PATTERNS.md
    - frontend/src/components/App.tsx
    - backend/Code.js
  </read_first>
  <behavior>
    - Cập nhật App.tsx để trích xuất tham số launch `view=chart` từ URL query string hoặc `startParam` từ initData của Telegram SDK.
    - Quản lý trạng thái hiển thị/collapsible của GrowthChart ở trên cùng màn hình khi tham số này được kích hoạt (D-05).
    - Cập nhật hàm `handleTelegramWebhook` trong backend/Code.js để xử lý lệnh `/chart` từ người dùng, đọc `MINI_APP_URL` và ghép tham số `view=chart` vào URL đích của nút mở ứng dụng gửi về Telegram (D-06).
  </behavior>
  <action>
    Cập nhật App.tsx và backend/Code.js để đồng bộ luồng kích hoạt và định tuyến biểu đồ qua query parameter (per D-05, D-06).
  </action>
  <verify>
    <automated>grep -q "view=chart" backend/Code.js && grep -q "viewParam === 'chart'" frontend/src/components/App.tsx</automated>
  </verify>
  <done>Định tuyến tham số biểu đồ từ Telegram Bot Webhook tới Mini App React thành công.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Telegram Client -> Web App Viewport | Tham số view=chart truyền qua URL của Web App nút bấm được Telegram hiển thị. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| SEC-05 | Information Disclosure | Growth Chart Data | Low | Mitigated | Toàn bộ dữ liệu tính toán và biểu diễn trên biểu đồ được xử lý hoàn toàn ở phía client của thiết bị người dùng thông qua mã nguồn đóng gói, không đẩy dữ liệu trung gian lên các dịch vụ bên thứ ba. |
| SEC-06 | Tampering | Telegram Web App Link | Low | Mitigated | Đường dẫn URL của ứng dụng (`MINI_APP_URL`) được quản lý an toàn trong Script Properties của Google Apps Script, không chấp nhận việc sửa đổi đường dẫn từ payload yêu cầu của webhook. |
</threat_model>
