---
phase: 04-frontend-ui-twa-auth
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - frontend/src/utils/dateMask.ts
  - frontend/src/utils/interest.ts
  - frontend/src/components/BottomSheet.tsx
  - frontend/src/components/DepositCard.tsx
  - frontend/src/components/DepositList.tsx
  - frontend/src/components/DepositForm.tsx
  - frontend/src/components/App.tsx
autonomous: true
requirements:
  - UI-02
  - DEP-01
must_haves:
  truths:
    - "Danh sách hiển thị dưới dạng card compact chỉ các khoản gửi active hoặc matured của người dùng hiện tại có ngày đáo hạn còn lại <= 3 ngày hoặc đã quá hạn (D-09, D-10, D-12)."
    - "Nhấn vào card mở chi tiết đầy đủ trong bottom sheet / slide-up panel (D-13)."
    - "Các khoản tiết kiệm hiển thị trong danh sách được sắp xếp theo trình tự ngày đáo hạn tăng dần (gần nhất lên đầu) (D-11)."
    - "Form thêm mới khoản gửi được mở từ nút bấm nổi FAB '+' góc dưới phải (D-14)."
    - "Form thêm mới tự động định dạng chuỗi ngày nhập vào theo dạng DD/MM/YYYY bằng date mask khi gõ phím, không dùng datepicker (D-15)."
    - "Form thực hiện validation inline realtime (số tiền > 0, lãi suất >= 0, ngày hợp lệ, ngày đáo hạn sau ngày tạo) (D-16)."
    - "Form hiển thị chính xác số ngày gửi thực tế và số tiền lãi dự kiến realtime ngay khi người dùng điền đủ thông tin hợp lệ (D-17)."
    - "Hành vi sau khi thêm mới thành công: Form tiếp tục mở để người dùng nhập tiếp, hiển thị thông báo toast và tự động làm mới danh sách dữ liệu phía sau (D-18)."
  artifacts:
    - "frontend/src/utils/dateMask.ts"
    - "frontend/src/utils/interest.ts"
    - "frontend/src/components/BottomSheet.tsx"
    - "frontend/src/components/DepositForm.tsx"
  key_links:
    - "DepositForm.tsx sử dụng callBackendApi để gửi yêu cầu add_deposit và sau đó gọi callback refresh dữ liệu truyền từ App.tsx."
    - "DepositList.tsx sử dụng các hàm helper trong interest.ts để tính toán số ngày đáo hạn còn lại và lọc các khoản cần xử lý."
---

<objective>
Xây dựng giao diện hiển thị danh sách các khoản gửi cần xử lý (sắp đáo hạn ≤ 3 ngày và quá hạn), component Bottom Sheet hiển thị chi tiết khoản gửi, và Form thêm mới khoản gửi tiết kiệm trực quan kích hoạt bởi nút FAB nổi. Form tích hợp tính năng tự động định dạng ngày DD/MM/YYYY, kiểm tra dữ liệu đầu vào và xem trước lãi suất dự kiến realtime.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-ui-twa-auth/04-CONTEXT.md
@.planning/phases/04-frontend-ui-twa-auth/04-RESEARCH.md
@.planning/phases/04-frontend-ui-twa-auth/04-PATTERNS.md
- frontend/src/components/App.tsx
</context>

<tasks>
<task type="auto">
  <name>Xây dựng helper định dạng ngày tháng và tính lãi dự toán realtime</name>
  <files>
    - frontend/src/utils/dateMask.ts
    - frontend/src/utils/interest.ts
  </files>
  <read_first>
    - .planning/phases/04-frontend-ui-twa-auth/04-PATTERNS.md
    - frontend/src/utils/dateMask.ts
    - frontend/src/utils/interest.ts
  </read_first>
  <behavior>
    - Triển khai hàm formatMaskDate(value, prevValue) tự động định dạng và chèn dấu gạch chéo '/' khi nhập ngày, hỗ trợ xử lý phím xóa ký tự phân tách.
    - Triển khai hàm parseClientDateString(dateStr) để tạo đối tượng Date không bị lệch múi giờ trên client.
    - Triển khai hàm calculateDaysBetween(startStr, endStr) tính số ngày chênh lệch thực tế giữa hai mốc ngày dạng chuỗi DD/MM/YYYY.
    - Triển khai hàm calculateExpectedInterest(amount, rate, startStr, endStr) tính toán lãi dự kiến theo công thức chuẩn: gốc * (lãi suất / 100) * (ngày / 365).
  </behavior>
  <action>
    Tạo hai tệp mới frontend/src/utils/dateMask.ts và frontend/src/utils/interest.ts chứa các hàm xử lý logic toán học và định dạng như mô tả.
  </action>
  <verify>
    <automated>grep -q "formatMaskDate" frontend/src/utils/dateMask.ts && grep -q "calculateExpectedInterest" frontend/src/utils/interest.ts</automated>
  </verify>
  <done>Các helper xử lý định dạng ngày và tính lãi toán học được định nghĩa đầy đủ và chính xác.</done>
</task>

<task type="auto">
  <name>Xây dựng component BottomSheet với CSS transition</name>
  <files>
    - frontend/src/components/BottomSheet.tsx
  </files>
  <read_first>
    - .planning/phases/04-frontend-ui-twa-auth/04-RESEARCH.md
    - frontend/src/components/BottomSheet.tsx
  </read_first>
  <behavior>
    - Viết component BottomSheet nhận các props: isOpen, onClose, title, children.
    - Sử dụng backdrop nền mờ màu đen có độ đục (bg-black/50) trượt mượt mà (opacity-0 sang opacity-100).
    - Container của sheet trượt từ dưới lên (translate-y-full sang translate-y-0) thông qua việc quản lý state CSS transition thời lượng 300ms.
    - Cho phép đóng sheet bằng cách click ra ngoài backdrop hoặc nhấn nút dấu x.
  </behavior>
  <action>
    Tạo tệp frontend/src/components/BottomSheet.tsx sử dụng React hook useState/useEffect để tạo trễ unmount khớp với thời gian chuyển động CSS (300ms).
  </action>
  <verify>
    <automated>test -f frontend/src/components/BottomSheet.tsx && grep -q "translate-y" frontend/src/components/BottomSheet.tsx</automated>
  </verify>
  <done>Component BottomSheet được cấu hình hoàn chỉnh có hiệu ứng mượt mà đúng chuẩn thiết kế Mini App.</done>
</task>

<task type="auto">
  <name>Phát triển component danh sách và chi tiết khoản gửi</name>
  <files>
    - frontend/src/components/DepositCard.tsx
    - frontend/src/components/DepositList.tsx
  </files>
  <read_first>
    - .planning/phases/04-frontend-ui-twa-auth/04-CONTEXT.md
    - frontend/src/components/DepositCard.tsx
    - frontend/src/components/DepositList.tsx
  </read_first>
  <behavior>
    - Thiết kế DepositCard hiển thị thông tin compact: Số tiền gửi (định dạng VND vi-VN), Lãi suất %, Ngày đáo hạn, trạng thái (quá hạn bao nhiêu ngày hoặc còn lại bao nhiêu ngày).
    - Thiết kế DepositList lọc các khoản từ API: chỉ lấy các khoản ở trạng thái "active" hoặc "matured" có ngày đáo hạn còn lại so với hôm nay <= 3 ngày hoặc đã quá hạn.
    - Sắp xếp các khoản gửi theo ngày đáo hạn tăng dần để đưa khoản cần xử lý khẩn cấp lên đầu.
    - Khi nhấn vào một Card, mở BottomSheet hiển thị chi tiết đầy đủ thông tin: ID, Số tiền gốc, Lãi suất, Trạng thái, Lãi dự kiến, Ngày tạo, Ngày đáo hạn, Số ngày gửi thực tế.
  </behavior>
  <action>
    Tạo mới tệp frontend/src/components/DepositCard.tsx và frontend/src/components/DepositList.tsx. Import BottomSheet để hiển thị panel chi tiết khi người dùng tương tác click vào card.
  </action>
  <verify>
    <automated>test -f frontend/src/components/DepositCard.tsx && grep -q "DepositCard" frontend/src/components/DepositList.tsx</automated>
  </verify>
  <done>Danh sách hiển thị đúng bộ lọc cần xử lý và hỗ trợ mở popup chi tiết khi click vào từng khoản.</done>
</task>

<task type="auto">
  <name>Thiết kế Form thêm mới khoản gửi tích hợp inline validation và preview lãi</name>
  <files>
    - frontend/src/components/DepositForm.tsx
  </files>
  <read_first>
    - .planning/phases/04-frontend-ui-twa-auth/04-PATTERNS.md
    - frontend/src/components/DepositForm.tsx
  </read_first>
  <behavior>
    - Xây dựng DepositForm hiển thị dưới dạng Bottom Sheet.
    - Gồm các trường nhập: Số tiền gốc (type number), Lãi suất (type number), Ngày tạo (DD/MM/YYYY), Ngày đáo hạn (DD/MM/YYYY).
    - Tích hợp hàm formatMaskDate vào sự kiện onChange của các ô nhập ngày.
    - Thực hiện inline validation khi blur ô nhập liệu: Số tiền > 0, Lãi suất >= 0, Ngày tạo và Ngày đáo hạn đúng định dạng và Ngày đáo hạn phải sau Ngày tạo. Hiển thị thông báo lỗi nhỏ màu đỏ bên dưới ô nhập.
    - Khi thông tin điền đầy đủ và hợp lệ, hiển thị Preview tính toán Số ngày gửi và Tiền lãi dự kiến trực quan ngay trước nút lưu.
    - Khi submit thành công, gọi API add_deposit, hiển thị toast thông báo, giữ form mở để tiếp tục thêm và gọi callback refresh dữ liệu.
  </behavior>
  <action>
    Tạo tệp frontend/src/components/DepositForm.tsx, import callBackendApi và các helper xử lý ngày/lãi để hoàn thiện giao diện thêm mới.
  </action>
  <verify>
    <automated>test -f frontend/src/components/DepositForm.tsx && grep -q "calculateExpectedInterest" frontend/src/components/DepositForm.tsx</automated>
  </verify>
  <done>Form thêm mới tích hợp đầy đủ tính năng validate dữ liệu đầu vào, hiển thị lãi preview realtime và gửi dữ liệu thành công.</done>
</task>

<task type="auto">
  <name>Tích hợp liên kết hệ thống tại App.tsx</name>
  <files>
    - frontend/src/components/App.tsx
  </files>
  <read_first>
    - frontend/src/components/App.tsx
    - .planning/phases/04-frontend-ui-twa-auth/04-CONTEXT.md
  </read_first>
  <behavior>
    - Cập nhật App.tsx để tải dữ liệu danh sách khoản tiết kiệm qua API get_deposits của user đã chọn.
    - Render danh sách DepositList truyền dữ liệu tải về.
    - Hiển thị Floating Action Button (FAB) dấu "+" ở góc dưới bên phải màn hình khi người dùng đã đăng nhập để kích hoạt mở DepositForm Bottom Sheet.
    - Truyền hàm reload dữ liệu làm callback cho DepositForm để tự động cập nhật danh sách phía sau mà không cần tải lại trang web.
  </behavior>
  <action>
    Chỉnh sửa frontend/src/components/App.tsx, bổ sung các state quản lý danh sách deposits, loading state, và hàm tải dữ liệu fetchDeposits() cập nhật lại sau khi thêm mới thành công.
  </action>
  <verify>
    <automated>grep -q "get_deposits" frontend/src/components/App.tsx && grep -q "DepositList" frontend/src/components/App.tsx</automated>
  </verify>
  <done>App.tsx đóng vai trò trung tâm điều phối dữ liệu và liên kết toàn bộ luồng nghiệp vụ hoạt động hoàn hảo.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User Form Inputs -> Client App State | Ranh giới dữ liệu nhập vào từ bàn phím người dùng. Cần được lọc bỏ các ký tự lạ và kiểm tra kiểu dữ liệu trước khi xử lý tính toán hoặc lưu trữ. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| SEC-05 | Tampering | Client Form Data | Medium | Mitigated | Thực hiện xác thực kép (Dual Validation): Phía client kiểm soát định dạng ngày, kiểu số dương và hiển thị lỗi trực quan; Phía backend (Code.js) thực hiện parse và xác thực lại toàn bộ dữ liệu trước khi appendRow vào Google Sheets để chặn các request chỉnh sửa payload trực tiếp. |
| SEC-06 | Information Disclosure | Deposit detail view | Low | Mitigated | Chỉ hiển thị chi tiết các khoản gửi thuộc về username_bankcode đã chọn và được xác thực trùng khớp qua session initData tương ứng, ngăn chặn việc truy cập chéo dữ liệu của người dùng khác. |
</threat_model>
